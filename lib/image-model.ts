// One explicit model id for every Dinner Spinner-generated dish image.
// Keep this raw id instead of a Nex alias so upstream alias changes cannot
// silently reroute generations to another provider or quality tier.
export const DISH_IMAGE_MODEL = "gpt-image-2";
